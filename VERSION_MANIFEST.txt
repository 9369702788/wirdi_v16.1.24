WIRDI -- cumulative diff: everything that changed since Wirdi_v243.zip
================================================================================
Apply this on top of your current v243 build (v244 through v248 merged and
de-duplicated into one clean package):

lib/features/mushaf/mushaf_view_screen.dart        (v244) continuous-scroll stability fix: pinch-zoom removed from that mode only, kept in single-page mode
lib/features/home/home_dashboard_screen.dart       (v245+v248 MERGED) hadith text no longer truncated + tap goes to the correct hadith + moon phase image/name added to next-prayer card
lib/features/hadith/hadith_collection_screen.dart  (v245) fixed lazy-list scroll-to-target bug
lib/core/services/mathhab_service.dart             (v246) default Mathhab Hanafi -> Shafi'i (fixes Asr ~1hr late)
lib/features/settings/settings_screen.dart         (v246) matching Mathhab default + carries forward 2 earlier fixes (permission race condition, floating SnackBar)
lib/core/services/moon_calculator.dart             (v247) added phaseImageAsset()
lib/features/moon/moon_screen.dart                 (v247) uses real moon images now
pubspec.yaml                                       (v247) registers assets/images/moon_phases/
assets/images/moon_phases/*.webp (8 files)         (v247) the actual moon phase images
lib/features/prayer/prayer_times_screen.dart       (v248) moon phase image/name added to next-prayer card

IMPORTANT: home_dashboard_screen.dart was touched in BOTH v245 and v248.
This package contains the properly MERGED final version with both sets of
changes -- do not use the home_dashboard_screen.dart from v245 or v248
individually, use only the one in THIS package.
