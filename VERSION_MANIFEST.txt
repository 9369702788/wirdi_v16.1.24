WIRDI v242 -- changes since the "Wirdi_Mushaf_And_Notification_Fixes_Combined.zip" baseline
================================================================================

Files in THIS package (only files that changed -- apply on top of your
current build, nothing else needs to change):

1) lib/features/mushaf/mushaf_view_screen.dart
   - Fixes blank page in continuous-scroll mode (unbounded height bug)
   - Fixes continuous-scroll getting stuck / not scrolling between pages
     (InteractiveViewer/ListView gesture conflict)
   - Restores pinch-zoom (2+ fingers) working alongside continuous scroll
   - This SUPERSEDES 3 earlier intermediate zips sent during debugging:
     Wirdi_Mushaf_Scroll_Conflict_Fix.zip,
     Wirdi_Mushaf_Continuous_Scroll_Real_Fix_v2.zip -- only USE this v242
     package, ignore those two, they're obsolete intermediate steps.

2) lib/core/data/radio_stations.dart (NEW change, not in the baseline)
   - Removed 19 fabricated-looking placeholder station URLs from the
     offline fallback list (vanity slugs like 'kuwait-quran' that don't
     match how radiojar.com/zeno.fm actually issue stream URLs)

3) lib/core/services/radio_service.dart (NEW change, not in the baseline)
   - Broadened Radio-Browser search from 1 tag ('quran') to 6 tags
     ('quran','islam','islamic','coran','tilawah','quran radio'),
     merged + deduplicated
   - Fixed a mislabeling bug: a successful multi-source merge was being
     reported with the same enum value as "all sources failed"

NOT included here (unchanged since your baseline, already applied):
   - lib/core/services/notification_service.dart
   - lib/features/settings/settings_screen.dart

Going forward, patches will be numbered sequentially starting from v242.
